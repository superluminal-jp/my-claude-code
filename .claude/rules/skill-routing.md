# Skill Routing

- Code implementation or behavior changes -> load `coder`.
- Produced artifacts (docs, `docs/` folder, decks, charts, translation, editing) -> load `editor`.
- Any ambiguity in requirements -> load `clarifier`.
